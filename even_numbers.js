//Question: Print all even numbers between 1 and 20 using a while loop.
//solution

let num = 2; // Start with the even number 2 because limit given 1 to 20 

while (num <= 20) {
  console.log(num);
  num += 2; 
}